package zHOMEWORK;

import java.sql.SQLOutput;
import java.util.Scanner;

public class h14_March22 {
    public static void main(String[]args) {
   /*     Scanner input=new Scanner(System.in);
        System.out.println("Be ready for the magic!!! Enter a number below and I will give you the reverse of it!!!");
        String list=input.nextLine();
        int lastIndex=list.length()-1;
        String reverseList=" ";
        for (int i=lastIndex;i>=0; i--){

            reverseList+=list.charAt(i);}
            System.out.println("The magical number is: "+reverseList);
        Scanner console = new Scanner(System.in);
        int number;
        char choice;
        int even=0;
        int odd=0;
        do
        {System.out.println("Enter a number: ");
            number = console.nextInt();
            if (number%2 == 0){
            even+=number;}
        else {odd+=number;}
            System.out.println("You can enter another one will you?");
            choice=console.next().charAt(0);}
            while (choice=='Y'||choice=='y');
        System.out.println("Sum of even numbers: "+even);
        System.out.println("Sum of odd numbers: "+odd);
    }
}        Scanner scanner = new Scanner(System.in);
        System.out.println("Please enter a positive number: ");
        int input = scanner.nextInt();
        if (input < 0) {
            System.out.println("The number you entered is not a positive number but no worries, we switched it to a positive.");
            input = Math.abs(input);
        }
        String message = " ";
        for (int i = 1; i <= input; i++) {
            if (input % i != 0) {
                message += "The number you entered is a 'Prime Number'";
            } else {
                message += "The number you entered is not a 'Prime Number'";
            }

        break;
    }
        System.out.println(message);
    }}
Scanner word=new Scanner(System.in);
        System.out.println("Please enter a word, and i will give you the middle characters in uppercase");
        String wordR=word.nextLine();
        if (wordR.length()%2==0) {
            System.out.println(wordR.substring(wordR.length() / 2 - 1, wordR.length() / 2 + 1).toUpperCase());
        }else{
            System.out.println(wordR.substring(wordR.length() / 2, wordR.length() / 2 + 1).toUpperCase());
        }

        Scanner input=new Scanner(System.in);
        System.out.println("Please enter a word");
        String w1=input.next();
        System.out.println("Please enter another word");
        String w2=input.next();
        System.out.println("The words you entered are: " +w1+ " and " +w2);
        if (w1.charAt(w1.length()-1)==(w2.charAt(0))){
            System.out.println("Last letter of the first and the first letter of the last word are same letters, so i will print below both words as one");
            System.out.println(w1+w2.substring(1));
        }

        System.out.println("Between 0 and 100, the odd numbers that can be divided by 3 and 5 are below ");
        for (int n = 1; n <= 100; n++) {
            if ((n % 2 != 0) && (n % 3 == 0) && (n % 5 == 0)) {
                System.out.println(n);

            }
           } System.out.println("************");
        System.out.println("Between 0 and 100, the even numbers that can be divided by 3 and 5 are below");
        for (int n = 1; n <= 100; n++) {
            if ((n % 2 == 0) && (n % 3 == 0) && (n % 5 == 0)) {
                System.out.println(n);
    }}

        Scanner scanner = new Scanner(System.in);
        System.out.println("Enter number to find it's factorial :");
        int value = scanner.nextInt();
        int factorial = value;
        for (int i=value-1; i>0 ;i--){
            factorial = factorial*i;
        }
        System.out.println("Factorial of the number you entered is: "+factorial);
*/


    }}